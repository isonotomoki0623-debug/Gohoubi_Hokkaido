<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.ProductMapper">

	<!--商品一覧で、商品情報とエリア名を表示 -->
	<select id="findAll" resultType="com.example.demo.entity.Product">

		SELECT
		p.*,
		h.name AS hokkaido_area_name
		FROM products p
		JOIN hokkaido_areas h
		ON p.hokkaido_area_id = h.id

	</select>

	<select id="findById"
		resultType="com.example.demo.entity.Product">
		SELECT id, name, price, review_star, stock
		FROM products
		WHERE id = #{id}
	</select>

</mapper>
