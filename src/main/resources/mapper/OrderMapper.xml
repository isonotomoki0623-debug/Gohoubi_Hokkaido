<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.OrderMapper">

	<insert id="InsertOrder" parameterType="com.example.demo.entity.Order" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO orders (user_id, buy_at, total_amount, status, coupon_id)
			VALUES (#{userId}, CURRENT_DATE, #{totalAmount}, false, #{couponId})
	</insert>
	
	<insert id="InsertOrderItems">
		INSERT INTO order_items (order_id, product_id, quantity)
			VALUES (#{orderId}, #{productId}, #{quantity})
	</insert>

	<select id="hasCoupons" resultType="com.example.demo.entity.Coupon">
		SELECT *
		FROM coupons
		WHERE id IN (SELECT id FROM coupons_users WHERE user_id = #{userId})
	</select>

</mapper>
